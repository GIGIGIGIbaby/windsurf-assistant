# 道Agent — Windsurf ACP 自由模型注入

> 道法自然 · 无为而无不为 · 通过官方 ACP 协议注入任意第三方模型

## 原理

Windsurf 的 **Agent Client Protocol (ACP)** 是官方的 Agent 扩展机制。
Devin 就是通过 ACP 接入 Windsurf 的。道Agent 做同样的事 — 只是用你自己选择的模型。

```
Windsurf IDE ←→ dao-agent (stdio/ndjson) ←→ 第三方 LLM API
     │                                         │
     ├─ 工具执行 (fs/read, fs/write, terminal)  │
     │                                         ├─ Claude / GPT / Gemini
     └─ UI 渲染 (流式文本/工具结果)              └─ DeepSeek / Kimi / 本地模型
```

## 技术突破 — Unleash 特性门绕过

Windsurf 的 ACP custom agent 功能被 **Unleash 特性标志** 门控:

```javascript
// extension.js 逆向 — isAcpAgentFamilyUnleashEnabled("custom")
return !!(isWindsurfInsiders() || isWindsurfNext() || hasDevExtension() || isDevelopment())
  || (ACP_ENABLED && acp-custom-enabled)  // 服务端标志,不可控
```

**突破路径**: `hasDevExtension()` 检查 `Codeium.codeium-dev` 扩展是否存在:
```javascript
function hasDevExtension() {
  return void 0 !== vscode.extensions.getExtension("Codeium.codeium-dev");
}
```

在 `~/.windsurf/extensions/codeium.codeium-dev-0.0.1/` 放置一个 no-op stub 扩展即可。
`setup.js` 会自动完成此操作。

## 快速开始

### 1. 安装注册

```powershell
cd E:\道\道生一\一生二\Windsurf万法归宗\dao-agent
node setup.js
```

### 2. 重载 Windsurf

`Ctrl+Shift+P` → `Reload Window`

### 3. 使用

`Ctrl+Shift+/` (Agent Selector) → 选择 **道Agent**

## 配置

### 环境变量 (通过 Windsurf Settings)

在 `settings.json` 中添加:

```json
{
  "windsurf.acp.agentEnv": {
    "dao-agent": {
      "DAO_PROVIDER": "ag",
      "DAO_MODEL": "claude-sonnet-4-6"
    }
  }
}
```

### 支持的 Provider

| Provider | DAO_PROVIDER | 默认模型 | 说明 |
|----------|-------------|---------|------|
| AG Relay | `ag` | claude-sonnet-4-6 | 万法归宗本地路由 (默认) |
| Anthropic | `anthropic` | claude-sonnet-4-20250514 | 直连 Anthropic API |
| OpenAI | `openai` | gpt-4.1 | 直连 OpenAI API |
| DeepSeek | `deepseek` | deepseek-chat | 直连 DeepSeek API |
| OpenRouter | `openrouter` | anthropic/claude-sonnet-4 | 通过 OpenRouter |

### 所有环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DAO_PROVIDER` | `ag` | LLM 提供商 |
| `DAO_MODEL` | 因 provider 而异 | 模型名称 |
| `DAO_API_KEY` | (从 env 读取) | API Key |
| `DAO_API_BASE` | (因 provider 而异) | API 基地址 |
| `DAO_MAX_TURNS` | `30` | Agent Loop 最大轮次 |
| `DAO_TIMEOUT_MS` | `120000` | 请求超时 (ms) |
| `DAO_LOG_LEVEL` | `info` | 日志级别 (error/warn/info/debug/trace) |

## 工具支持

dao-agent 通过 ACP 协议请求 Windsurf IDE 执行工具:

- **read_file** → `fs/read_text_file`
- **write_file** → `fs/write_text_file`
- **run_command** → `terminal/create`
- **list_directory** → `terminal/create` (ls/dir)
- **grep_search** → `terminal/create` (grep/findstr)

## 管理命令

```powershell
node setup.js           # 安装/更新注册
node setup.js --check   # 检查状态
node setup.js --remove  # 移除注册

node test_acp.js        # 本地协议测试
```

## 文件结构

```
dao-agent/
├── index.js      # ACP Server 主体
├── setup.js      # 安装/管理脚本
├── test_acp.js   # 协议测试工具
├── package.json
└── README.md
```
