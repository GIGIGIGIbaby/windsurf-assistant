name: dao-vm-loop-b
# 印273 · slot-b · 永续VM · 无为而无不为
on:
  workflow_dispatch:
    inputs:
      duration_min:
        description: 'Keep-alive minutes'
        default: '300'
  schedule:
    - cron: '20 */4 * * *'   # offset 20min
  push:
    paths:
      - 'trigger-b.txt'

permissions:
  contents: write
  actions: write

concurrency:
  group: dao-vm-slot-b
  cancel-in-progress: false

jobs:
  vm-slot-b:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    env:
      VM_SLOT: b

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install cloudflared
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -O /usr/local/bin/cloudflared && chmod +x /usr/local/bin/cloudflared
      - name: Start VM + Tunnel
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VM_SLOT: b
        run: bash scripts/dao_vm_gha_bootstrap.sh b ${{ github.event.inputs.duration_min || '300' }}
      - name: Trigger next
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sf -X POST -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/dao-vm-loop-b.yml/dispatches" \
            -d '{"ref":"main"}' || true
