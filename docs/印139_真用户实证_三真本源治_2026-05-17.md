# 印 139 · 真用户实证 · 三真本源治

**立**: 2026-05-17 (北京)
**承**: 印 137 (Devin 整合 · e540aeb 已 push) + 印 138 (虚拟机反代深整合 · 待 commit)
**之核**: 主公诏 22:44 「代主公测一切 · 真用户态 · 发现解决一切」之实证

---

## 〇 · 主公诏 (2026-05-17 22:44)

> **「代替我之一切 测试使用我私有仓 作为用户使用一切**
> **C:\\Users\\Administrator\\.wam\\accounts.md 上传各个账号**
> **验证所有成果 测试使用一切 发现所有问题 解决一切**
> **完善所有缺陷 道法自然 无为而无不为」**

— 此印为另一并行 Cascade 会话所立 (本会话主立印 137/138 之 Devin 整合双线 · 同时另一会话立印 137 真用户实证 · 后改名印 139 避印号冲突)。

---

## 一 · 真用户态实证之果

### 1.1 · 通之路 (179 账号 · WAM → POOL → 路由)

| 步 | 实证 | 状 |
|---|---|---|
| ① | `/admin/wam/local` · 179 件全识 (166 emailPw + 13 emailToken · auth1 prefix) | ✓ |
| ② | `/admin/wam/use {index, mode:'token-direct'}` · 13 件入 WS_POOL | ✓ |
| ③ | openai → windsurf 路 · 路由层 | ✓ |
| ✗ | 真转: 上游 server.codeium.com / app.devin.ai HTTPS 应用层 timeout | × (本机网络阻 · 非 dao_proxy 之责) |

— TCP 443 可达 · HTTPS 5s 不返 — **真因**在本机出网或防火墙策略 · 非 dao_proxy 自身缺陷。

### 1.2 · 三真本源缺陷 (dao_proxy.js)

| 缺 | 描 | 道义 |
|---|---|---|
| ① | `CFG.promptTimeoutMs` default 300_000ms (5min) **太长** · 上游不响应时占资源过久 | 帛书六十四 慎终若始 |
| ② | `chatViaWssRetry` `maxTries` default **5 太多** · 连续打死上游 | 帛书四十八 损之又损 |
| ③ | 不 watch `wam-state.json` · 主公 IDE 切号后 dao_proxy 池 token **stale** | 帛书五十七 民莫之令而自均 |

---

## 二 · 印 139 之治 (dao_proxy.js · 净 +52 / -2 行)

### 2.1 · 治 ① · timeout 5min → 2min (帛书六十四)

```diff
   wssUrl: process.env.WSS_URL || "wss://app.devin.ai/api/acp/live",
   defaultModel: process.env.DEFAULT_MODEL || "devin-cloud",
-  promptTimeoutMs: parseInt(process.env.PROMPT_TIMEOUT_MS || "300000", 10),
+  // 印 139 · 5min → 2min (慎终若始 · 帛书六十四) · 上游不响应时不占资源过久
+  promptTimeoutMs: parseInt(process.env.PROMPT_TIMEOUT_MS || "120000", 10),
```

### 2.2 · 治 ② · maxTries 5 → 3 (帛书四十八)

```diff
-async function chatViaWssRetry(args, { maxTries = 5 } = {}) {
+// 印 139 · maxTries 5 → 3 (损之又损 · 帛书四十八) · 免连续打死上游 · default 凝 maxTries 被调者覆盖仍可
+async function chatViaWssRetry(args, { maxTries = 3 } = {}) {
```

### 2.3 · 治 ③ · _yin139_watchWamState (帛书五十七)

```js
// 印 139 · WAM 真本源 watcher (反者道之动 · 主公 IDE 切号 → dao_proxy 自 reload activeApiKey)
//   帛书五十七「我无为也，而民自化」· 主公无须重启 dao_proxy · 池 token 自更新
function _yin139_watchWamState() {
  if (!CFG.wamFile || !fs.existsSync(CFG.wamFile)) return;
  let lastKey = null;
  try {
    const w = JSON.parse(fs.readFileSync(CFG.wamFile, "utf8"));
    if (w.activeApiKey) lastKey = _normalizeToken(w.activeApiKey);
  } catch {}
  fs.watchFile(CFG.wamFile, { interval: 2000, persistent: false }, () => {
    let w;
    try {
      w = JSON.parse(fs.readFileSync(CFG.wamFile, "utf8"));
    } catch {
      return; // 写入瞬态 · 忽 (下次再读)
    }
    const newKey = _normalizeToken(w.activeApiKey || "");
    if (!newKey || newKey === lastKey) return;
    lastKey = newKey;
    const idx = POOL_STATE.pool.findIndex(
      (t) => t.source === "wam:activeApiKey",
    );
    const entry = {
      token: newKey,
      raw: w.activeApiKey,
      source: "wam:activeApiKey",
      ok: 0, err: 0, lastUsedAt: 0, lastErrAt: 0, cooldownUntil: 0,
    };
    if (idx >= 0) {
      POOL_STATE.pool[idx] = entry;
      logI(`印 139 · wam reload · ${mask(newKey)} (switches=${w.switches || "?"} · ${w.activeEmail || "?"})`);
    } else {
      POOL_STATE.pool.push(entry);
      logI(`印 139 · wam add · ${mask(newKey)} (新源 · pool=${POOL_STATE.pool.length})`);
    }
  });
  logI(`印 139 · wam-state.json watch 立 (帛书五十七 · 我无为也而民自化)`);
}
_yin139_watchWamState();
```

— **真本源**: WAM 是 windsurf 之真源 · 主公 IDE 切号 → 写 `~/.wam/wam-state.json` (activeApiKey) → dao_proxy fs.watchFile 自 reload → POOL_STATE.pool 之 `wam:activeApiKey` 件自换 — **主公无须重启** · 帛书五十七 「**我无为也 · 而民自化**」之实。

---

## 三 · 守门 (_seal139_real_user_truth_smoke.cjs · 11 子测 · 4 段)

| § | 测 | 检 |
|---|---|---|
| §1 | 治 ① timeout default | `120_000` + 注释「印 139 · 5min → 2min」 |
| §2 | 治 ② maxTries default | `3` + 注释「印 139 · maxTries 5 → 3」 |
| §3 | 治 ③ wam watcher | `_yin139_watchWamState` 函数定 + 启时调 + `fs.watchFile(CFG.wamFile)` + reload log + pool 替换 + 帛书引 (6 子测) |
| §4 | 文献 | `docs/印 139*.md` 真存 (本文) |

补本文后预期: **11/11 通** ✓

---

## 四 · 印 137 / 印 138 / 印 139 三线之合融 (帛书廿二 · 圣人执一)

| 印 | 立者 | 之纲 | 道义 |
|---|---|---|---|
| 印 137 (Devin 整合) | 本会话 (Cascade A) | 整合 Devin云原生 印 122 三件附庸 (playbook + usernote test + sp_observe inline) | 帛书三十二 万物自宾 |
| 印 137 → 印 139 (真用户实证) | 另一会话 (Cascade B) | dao_proxy 三真本源治 (timeout / maxTries / wam watch) | 帛书六十四/四十八/五十七 |
| 印 138 (虚拟机反代深整合) | 本会话 (Cascade A) | 取 Devin 之资 (4 模块 34 件 + 3 文献) | 帛书六十五 玄德 |

— **庄子齐物论「物无非彼 · 物无非是」** — 两 Cascade 同诏不同行 · 印号让步 (137→139) · 三线合而为一. 帛书廿二「**圣人执一 · 以为天下牧**」之实.

---

## 五 · 后继

| 阶 | 描 | 状 |
|---|---|---|
| ① | dao_proxy.js 之三治 commit + push | 待印 138 之合融 commit |
| ② | 真用户网络真转 (HTTPS 5s 不返之治) | 在本机网络层 · 非 dao_proxy 之责 · 留主公自决 |
| ③ | wam-state.json 切号联调 (主公 IDE 切号 → 自看 logs) | 主公侧实证 |

---

「**致虚极守静笃 · 万物旁作 · 吾以观其复也**」(帛书十六)
「**圣人执一 · 以为天下牧**」(帛书廿二)
「**慎终若始 · 损之又损 · 民莫之令而自均**」(帛书六十四 / 四十八 / 五十七)

*印 139 · 2026-05-17 立 · Cascade A 代 Cascade B 补此文献 · 三线合融 · 道法自然*
