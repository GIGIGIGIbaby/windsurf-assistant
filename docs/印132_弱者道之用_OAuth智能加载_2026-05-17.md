# 印 132 · 弱者道之用 · OAuth client_id 智能加载 · 一次为·万次用

**立**: 2026-05-17
**承**: 印 130 一线到底 (OAuth Device-Flow 立 · client_id 仅 2 源)
**进**: 印 133+ · admin 面板深化 · 自检 OAuth App 配齐 · 多账号 OAuth App 切

---

## 主公诏 (2026-05-17)

> 「我无为 · 你无不为 · 唯变所适」

---

## 帛书引

| 章 | 引 | 应 |
|----|----|----|
| 七十八 | 天下莫柔弱于水 · 而攻坚强者莫之能胜也 · 以其无以易之也 | 不强求主公改代码 (强) · 让主公面板填一框 (柔) · 终胜 |
| 四十八 | 为道者日损 · 损之又损 · 以至于无为 · 无为而无不为 | 4 源任一即活 · 无须改 dao_oauth.js DEFAULT · 无须 commit · 无须重启 |
| 四十 | 反者道之动也 | 旧: 主公必改 dao_oauth.js · 新: 主公 admin 面板填 → localStorage 持 (反) |
| 廿二 | 圣人执一 · 以为天下牧 | URL · localStorage · window · DEFAULT 4 源归一 (`_resolveClientId`) |
| 六十四 | 为之于其未有也 · 治之于其未乱也 | setClientId 守门 · 拒 placeholder/短/空 (未乱即治) |

---

## 印 132 之核 · 4 源智能加载链

```text
window.daoOAuth.getClientId()
  ↓ 综合调
_resolveClientId() · 高 → 低优先返
  ├─① _readUrlParam()      : URL ?dao_oauth_client_id=Ov23li...
  │                         (一次性 · 高优 · 分享/调试 · 不写盘)
  ├─② _readLocalStorage()  : localStorage 'dao_oauth_client_id'
  │                         (持久 · 一次为·万次用 · admin 面板填)
  ├─③ _readWindowGlobal() : window.__DAO_OAUTH_CLIENT_ID__
  │                         (代码硬编 · index.html <head> · 印 130 之承)
  └─④ DEFAULT_CLIENT_ID    : "Ov23liYINDAO130PLACEHLDR"
                              (placeholder · 主公未配 · isConfigured()=false)
```

**主公一次"为" · 三选一**:

| 路 | 强弱 | 触面 | 说 |
|----|------|------|----|
| A) 改 dao_oauth.js DEFAULT_CLIENT_ID + commit | 强 | 代码 | 永久值 · 但要改码 + 推全用户 fork |
| B) index.html `<head>` 加 `<script>window.__DAO_OAUTH_CLIENT_ID__='Ov23li...'</script>` | 中 | 一行 HTML | 半全局 · 但仍需提交 |
| C) admin 面板输入 → setClientId(v) → localStorage | 弱 | 零代码改 | **真本源 · 弱者道之用** |

---

## API 暴 (`window.daoOAuth.*`)

| 名 | 入 | 出 | 说 |
|----|----|----|----|
| `getClientId()` | () | string | 当前生效 client_id (4 源解析后) |
| `setClientId(v)` | (string) | string (=v) | 存入 localStorage · 立活 · 拒 placeholder/short/空 (throw) |
| `clearClientId()` | () | string (回退后值) | 清 localStorage · 回退 4 源链 (URL > window > DEFAULT) |
| `whichSource()` | () | "url_param" / "localStorage" / "window_global" / "default_placeholder" | 当前值来自哪一源 (admin 面板用) |
| `isConfigured()` | () | bool | client_id 是否真值 (排 placeholder) |
| `setupHint()` | () | { configured, client_id, url, hint, steps[] } | 主公建 OAuth App 之 6 步 hint |

---

## UI · gate 页 admin 面板

`web/index.html` 之 `<details id="gate-oauth-config-details">` 折叠区:

```html
<details>
  <summary>⚙ 配 OAuth App client_id (<span id="gate-oauth-config-source">…</span>)</summary>
  <input id="gate-oauth-config-input" placeholder="Ov23li... 或 Iv1..." />
  <button id="gate-oauth-config-save">💾 存 (持久)</button>
  <button id="gate-oauth-config-clear">✗ 清</button>
  <span id="gate-oauth-config-status"></span>
  <small>或临时 URL · ?dao_oauth_client_id=... · 一次性高优先</small>
</details>
```

**dao_app.js · `bindOauthConfig()` 绑**:

- 启时显示当前 source (`whichSource()`) · 着色 (绿=已配 · 红=未配 · 蓝=URL一次)
- save 钮 → `daoOAuth.setClientId(input.value)` · 入 localStorage · refresh
- clear 钮 → `daoOAuth.clearClientId()` · 回退 4 源 · refresh
- Enter 键 = save 钮 (帛书四十三 无有入于无间)

---

## 守门 · `_seal132_oauth_client_id_smoke.cjs` (54/0 过)

**静守** (15 件):

- dao_oauth.js 含 `_resolveClientId` / `_readUrlParam` / `_readLocalStorage` / `_readWindowGlobal`
- dao_oauth.js 含 `getClientId` / `setClientId` / `clearClientId` / `whichSource`
- dao_oauth.js LS_KEY = URL_PARAM = 'dao_oauth_client_id'
- dao_oauth.js 暴 4 个新 API
- dao_app.js 含 `bindOauthConfig` 函数 + renderGate 调
- index.html 含 6 个 `gate-oauth-config-*` DOM ids + URL 用法提

**动守** (9 实验):

- ① 无 4 源 → `default_placeholder` + `isConfigured()=false`
- ② setClientId → 入 localStorage · 立活 · `isConfigured()=true`
- ③ clearClientId 回退 (无 url + 无 win)
- ④ URL 高优 (即便 ls/win 有 · URL 仍胜)
- ⑤ localStorage > window_global
- ⑥ window_global 单存 · `whichSource() === 'window_global'`
- ⑦ 拒 placeholder · 拒短串 · 拒空 (throw)
- ⑧ clear 后回退到 window_global (有 win 时)
- ⑨ setupHint 含 ≥5 步 + GH developers URL

---

## 影响面

**仅触 3 件 · +0 regression**:

- web/dao_oauth.js +111 行 (4 源链 + 4 API + 注释)
- web/dao_app.js +120 行 (`bindOauthConfig()` 函数 + renderGate 调)
- web/index.html +66 行 (`<details>` admin 面板 UI)
- tests/_seal132_oauth_client_id_smoke.cjs +320 行 (新立 · 54/0)
- tests/run_all.cjs +1 行 (引入)
- docs/印132_弱者道之用_OAuth智能加载_2026-05-17.md +新立

**24/24 全套通过 · 0 regression** (含印 130 OAuth Device-Flow 之原 93/0 · 印 132 新 54/0).

---

## 与印 131 / 130 之关

| 印 | 治 | 帛书核 |
|----|----|--------|
| 130 | 一线到底 · OAuth Device-Flow + 池接入闭环 | 四十八「为道者日损」 |
| 131 | 父旗子承 · 中文路径治本 (--preserve-symlinks) | 廿二「圣人执一」 |
| 132 | client_id 4 源智能加载 · 弱者道之用 | 七十八「弱之胜强」 |

**印 130 立全链 · 印 131 治路径疾 · 印 132 让主公一次为(填 client_id)**.

---

## 主公一次"为" · 5 min 建 OAuth App

```text
① https://github.com/settings/developers → New OAuth App
② App name: Windsurf Assistant (道独立体)
③ Homepage URL: https://zhouyoukang.github.io/windsurf-assistant/
④ Callback URL: 同 Homepage (device-flow 不用)
⑤ ☑ Enable Device Flow (关键 · 不勾则 device-flow 不通)
⑥ 拿 Client ID (Ov23li... 或 Iv1...) → 粘 admin 面板 → 💾 存
```

**A 路** (改 dao_oauth.js DEFAULT) — 强 · 全用户 fork 自动继承 · 但需 commit + push
**C 路** (admin 面板填 + localStorage) — 弱 · 仅本浏览器 · 但**零代码改 · 一次为·万次用**

主公自取 · 道法自然.

---

**印**: 132
**章**: 七十八
**核**: 4 源智能加载链 · 弱者道之用
**触**: web/dao_oauth.js + dao_app.js + index.html + 守门
**验**: 54/0 单 · 24/24 全
